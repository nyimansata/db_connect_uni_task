<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Appointment</title>

    <link rel="stylesheet" href="/styles/home.css" />
  </head>

  <body>
    <div class="container">
      <h1>Book An Appointment</h1>
      <p>Please let us know what we can do for you</p>

      <!-- CREATE FORM -->
      <form action="/api/v1/users" id="appointmentForm" method="POST">
        <div class="form-group">
          <label>Name</label>
          <input name="name" />
        </div>

        <div class="form-group">
          <label>Surname</label>
          <input name="lastName" />
        </div>

        <div class="form-group">
          <label>Email</label>
          <input type="email" name="email" />
        </div>

        <div class="form-group">
          <label>Message</label>
          <textarea name="message"></textarea>
        </div>

        <button type="submit">Submit</button>
      </form>
    </div>

    <!-- ---------- USER LIST ---------- -->
    <div class="subContainer">
      <h2>Submitted Appointments</h2>

      <% if (users.length === 0) { %>
      <p>No appointments yet.</p>
      <% } else { %>

      <table border="1" cellspacing="0" cellpadding="10" width="100%">
        <tr>
          <th>Name</th>
          <th>Surname</th>
          <th>Email</th>
          <th>Message</th>
          <th>Actions</th>
        </tr>

        <% users.forEach(user => { %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.lastName %></td>
          <td><%= user.email %></td>
          <td><%= user.message %></td>
          <td>
            <button onclick="editUser('<%= user._id %>')">Edit</button>
            <button onclick="deleteUser('<%= user._id %>')">Delete</button>
          </td>
        </tr>
        <% }) %>
      </table>

      <% } %>
    </div>

    <script src="/js/home.js"></script>
  </body>
</html>
